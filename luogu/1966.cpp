#include <cstdio>
#include <cstdlib>
#include <algorithm>

constexpr int maxn = 1e5+10;
long long a[maxn], b[maxn];
constexpr long long mod = 1e8-3;

int main(){
    int n;
    scanf("%d", &n);

    for (int i=0; i<n; i++){
        scanf("%lld", a+i);
    }
    for (int i=0; i<n; i++){
        scanf("%lld", b+i);
    }   

    
}